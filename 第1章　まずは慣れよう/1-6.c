#include <stdio.h>

int main(void) {

    //変数に整数値を代入して表示
    // int n;
    // n = 15;
    // printf("nの値は%dです。\n", n);

    //変数を整数値で初期化して表示

    int n = 20;
    printf("nの値は%dです。\n", n);

    return 0;
}

